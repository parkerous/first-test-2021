# Deploys the Soai website as a Cloudflare static-assets Worker.
# In your Cloudflare project's Build settings, set the Deploy command to:
#     npx wrangler deploy
name = "first-test-2021"
compatibility_date = "2024-11-01"

[assets]
directory = "./"
